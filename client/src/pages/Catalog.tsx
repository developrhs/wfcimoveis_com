import { ArrowLeft, ArrowUpRight, BedDouble, Check, ChevronDown, Filter, MapPin, Search, SlidersHorizontal, X } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { Link } from "wouter";
import { formatBRLFromCents } from "@/lib/currency";
import { fetchPublicProperties, propertyContactLink, PublicProperty } from "@/lib/publicProperties";

const WHATSAPP = "5562995111648";
const statusLabels = { disponivel: "Disponível", reservado: "Reservado", vendido: "Vendido" } as const;
function ImageOrEmpty({ src, alt }: { src: string | null; alt: string }) { return src ? <img src={src} alt={alt} /> : <div className="image-unavailable" role="img" aria-label="Imagem ainda não cadastrada" />; }

export default function Catalog() {
  const [properties, setProperties] = useState<PublicProperty[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  const [query, setQuery] = useState("");
  const [type, setType] = useState("Todos");
  const [saleType, setSaleType] = useState("Todos");
  const [status, setStatus] = useState("disponivel");
  const [minPrice, setMinPrice] = useState("");
  const [maxPrice, setMaxPrice] = useState("");
  const [sort, setSort] = useState("recentes");
  const [filtersOpen, setFiltersOpen] = useState(false);

  useEffect(() => { const controller = new AbortController(); fetchPublicProperties(controller.signal).then(setProperties).catch((reason: unknown) => { if ((reason as Error)?.name !== "AbortError") setError(reason instanceof Error ? reason.message : "Não foi possível carregar o catálogo."); }).finally(() => setLoading(false)); return () => controller.abort(); }, []);
  const types = [...new Set(properties.map((property) => property.type))];
  const saleTypes = [...new Set(properties.map((property) => property.saleType))];
  const filtered = useMemo(() => { const min = minPrice ? Number(minPrice) * 100 : 0; const max = maxPrice ? Number(maxPrice) * 100 : Number.POSITIVE_INFINITY; const result = properties.filter((property) => { const haystack = `${property.title} ${property.location} ${property.type}`.toLowerCase(); return (!query || haystack.includes(query.toLowerCase())) && (type === "Todos" || property.type === type) && (saleType === "Todos" || property.saleType === saleType) && (status === "todos" || property.status === status) && property.price >= min && property.price <= max; }); return [...result].sort((a, b) => sort === "menor" ? a.price - b.price : sort === "maior" ? b.price - a.price : 0); }, [maxPrice, minPrice, properties, query, saleType, sort, status, type]);

  return <div className="catalog-page"><header className="catalog-header"><div className="container catalog-nav"><Link href="/" className="catalog-back"><ArrowLeft size={17} /> Voltar para início</Link><Link href="/" className="catalog-logo"><img src="/manus-storage/logo-wfc_8d426acc.jpg" alt="WFC Imóveis" /></Link><div className="catalog-header-links"><Link href="/clientes">Nossos clientes</Link><a className="catalog-contact" href={`https://wa.me/${WHATSAPP}`} target="_blank" rel="noreferrer">WhatsApp <ArrowUpRight size={15} /></a></div></div></header><main className="catalog-main"><div className="container"><div className="catalog-intro"><div><p className="section-label">Catálogo WFC</p><h1>Encontre um lugar<br /><em>para chamar de seu.</em></h1><p>Imóveis publicados pela equipe WFC e carregados diretamente do banco de dados.</p></div><div className="catalog-count"><strong>{loading ? "…" : filtered.length}</strong><span>oportunidades<br />encontradas</span></div></div>{error && <div className="catalog-empty"><h2>Catálogo temporariamente indisponível</h2><p>{error}</p></div>}{loading && <div className="catalog-empty"><p>Carregando imóveis do banco de dados…</p></div>}{!loading && !error && <><div className="catalog-toolbar"><div className="catalog-search"><Search size={18} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Busque por título, bairro ou tipo" /></div><button className="filter-trigger" onClick={() => setFiltersOpen((value) => !value)}><SlidersHorizontal size={17} /> Filtros <span>{[type !== "Todos", saleType !== "Todos", status !== "todos", !!minPrice, !!maxPrice].filter(Boolean).length || ""}</span></button><label className="sort-select"><span>Ordenar:</span><select value={sort} onChange={(event) => setSort(event.target.value)}><option value="recentes">Mais recentes</option><option value="menor">Menor preço</option><option value="maior">Maior preço</option></select><ChevronDown size={15} /></label></div><div className="catalog-quick-filters"><button className={type === "Todos" ? "active" : ""} onClick={() => { setType("Todos"); setStatus("disponivel"); }}>Todos os imóveis</button>{types.map((value) => <button className={type === value ? "active" : ""} key={value} onClick={() => { setType(value); setStatus("disponivel"); }}>{value}</button>)}<button onClick={() => { setSaleType("Financiamento"); setStatus("disponivel"); }}>Financiamento</button><button onClick={() => setStatus("reservado")}>Reservados</button></div><div className={filtersOpen ? "catalog-layout filters-visible" : "catalog-layout"}><aside className="catalog-filters"><div className="filter-head"><strong>Refine sua busca</strong><button onClick={() => { setType("Todos"); setSaleType("Todos"); setStatus("todos"); setMinPrice(""); setMaxPrice(""); }}><X size={13} /> Limpar</button></div><label className="filter-field"><span>Tipo de imóvel</span><select value={type} onChange={(event) => setType(event.target.value)}><option>Todos</option>{types.map((value) => <option key={value}>{value}</option>)}</select></label><label className="filter-field"><span>Tipo de venda</span><select value={saleType} onChange={(event) => setSaleType(event.target.value)}><option>Todos</option>{saleTypes.map((value) => <option key={value}>{value}</option>)}</select></label><div className="filter-field"><span>Faixa de preço</span><div className="price-fields"><input type="number" min="0" value={minPrice} onChange={(event) => setMinPrice(event.target.value)} placeholder="Mínimo" /><input type="number" min="0" value={maxPrice} onChange={(event) => setMaxPrice(event.target.value)} placeholder="Máximo" /></div></div><div className="filter-field"><span>Status do imóvel</span><label className="radio-row"><input type="radio" checked={status === "disponivel"} onChange={() => setStatus("disponivel")} /> Apenas disponíveis</label><label className="radio-row"><input type="radio" checked={status === "todos"} onChange={() => setStatus("todos")} /> Todos os status</label></div></aside><section className="catalog-results"><div className="results-caption"><span><Filter size={14} /> {filtered.length} resultados</span><span>Dados atualizados pela equipe WFC</span></div><div className="catalog-grid">{filtered.map((property) => <article className="catalog-card" key={property.id} onClick={() => { window.location.href = `/imovel/${property.id}`; }} role="link" tabIndex={0}><div className="catalog-card-image"><ImageOrEmpty src={property.image} alt={property.title} /><span className={`status-pill ${property.status}`}>{property.status === "disponivel" && <Check size={12} />}{statusLabels[property.status]}</span><small>Cód. {property.id}</small></div><div className="catalog-card-body"><span className="catalog-card-type">{property.type} · {property.saleType}</span><h2>{property.title}</h2><p><MapPin size={14} /> {property.location}</p><div className="catalog-card-meta"><span><BedDouble size={15} /> {property.bedrooms ? `${property.bedrooms} quartos` : "Uso flexível"}</span><span>{property.area}</span>{property.total && <span>{property.available} de {property.total} disponíveis</span>}</div><div className="catalog-card-footer"><strong>{formatBRLFromCents(property.price)}</strong><a onClick={(event) => event.stopPropagation()} href={propertyContactLink(property, WHATSAPP)} target="_blank" rel="noreferrer">Tenho interesse <ArrowUpRight size={15} /></a></div></div></article>)}</div>{filtered.length === 0 && <div className="catalog-empty"><Filter size={25} /><h2>Nenhum imóvel encontrado</h2><p>Não há imóveis do banco correspondentes a essa busca.</p></div>}</section></div></>}</div></main></div>;
}
